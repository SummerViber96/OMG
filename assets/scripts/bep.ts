

const { ccclass, property } = cc._decorator;

@ccclass
export default class NewClass extends cc.Component {

    // @property(cc.Node)
    // fire: cc.Node = null;
    // @property(cc.Node)
    // khoi: cc.Node = null
    // @property(cc.Node)
    // icon: cc.Node = null;
    // // @property(cc.SpriteFrame)
    // // imgOff: cc.SpriteFrame
    // start() {

    // }
    // offBep() {
    //     this.fire.active = false;
    //     // this.node.getComponent(cc.Animation).stop();
    //     this.icon.getComponent(cc.Sprite).spriteFrame =this.imgOff;
    //     this.khoi.active = true;

    // }
    // update (dt) {}
}
